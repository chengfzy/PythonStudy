"""
Color defined in OpenCV
"""

import numpy as np

Black = np.array([0, 0, 0])
White = np.array([255, 255, 255])
Blue = np.array([255, 0, 0])
Green = np.array([0, 255, 0])
Red = np.array([0, 0, 255])
Magenta = np.array([255, 0, 255])
Cyan = np.array([255, 255, 0])
